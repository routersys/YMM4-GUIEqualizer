<UserControl x:Class="ymm4_guiequalizer.GuiEqualizerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             xmlns:local="clr-namespace:ymm4_guiequalizer"
             xmlns:theme="clr-namespace:YukkuriMovieMaker.Theme;assembly=YukkuriMovieMaker.Plugin"
             mc:Ignorable="d"
             Name="this">
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:BooleanConverter x:Key="NotNullConverter" />
    </UserControl.Resources>
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="縦軸ズーム" VerticalAlignment="Center" Margin="0,0,5,0">
                    <TextBlock.ToolTip>
                        <ToolTip>
                            <TextBlock>エディタの縦軸（ゲイン）の表示範囲を調整します。</TextBlock>
                        </ToolTip>
                    </TextBlock.ToolTip>
                </TextBlock>
                <Slider Name="ZoomSlider" Minimum="6" Maximum="48" Value="24" TickFrequency="6" IsSnapToTickEnabled="True"
                        ValueChanged="ZoomSlider_ValueChanged" Width="200"
                        ToolTip="エディタの縦軸（ゲイン）の表示範囲を調整します。"/>
            </StackPanel>
            <Button Grid.Column="1" Name="SettingsButton" Width="24" Height="24"
                    HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,0,5"
                    Click="SettingsButton_Click" ToolTip="GUIイコライザーの詳細設定を開きます。">
                <Path Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                      Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.65 15.48,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.52,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.52,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.48,18.68 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z" />
            </Button>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ToggleButton x:Name="PresetToggleButton" Grid.Column="0" Margin="0,0,5,5"
                          Content="{Binding SelectedPresetName, ElementName=this, FallbackValue='プリセットを選択...'}"
                          ToolTip="保存されたプリセットを選択または管理します。"/>

            <Button Grid.Column="1" Name="SavePresetButton" Content="保存" Width="50" Height="23" Margin="0,0,0,5"
                    Click="SavePresetButton_Click"
                    ToolTip="現在の設定をプリセットとして保存します。"/>

            <Popup x:Name="PresetPopup"
                   IsOpen="{Binding IsChecked, ElementName=PresetToggleButton, Mode=TwoWay}"
                   StaysOpen="False"
                   Placement="Bottom"
                   PlacementTarget="{Binding ElementName=PresetToggleButton}"
                   MinWidth="450"
                   MaxHeight="400"
                   AllowsTransparency="True" PopupAnimation="Fade">
                <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                        BorderBrush="{DynamicResource {x:Static theme:YMM4Colors.IconBorderBrushKey}}"
                        BorderThickness="1" CornerRadius="3">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ListBox Grid.Column="0" Name="GroupListBox"
                                 Background="Transparent" BorderThickness="0,0,1,0"
                                 BorderBrush="{DynamicResource {x:Static theme:YMM4Colors.IconBorderBrushKey}}"
                                 SelectionChanged="GroupListBox_SelectionChanged"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="8,5"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border Name="Border" Padding="{TemplateBinding Padding}" Background="Transparent" SnapsToDevicePixels="true">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="true">
                                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="true">
                                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ListBox Grid.Row="0" Name="PresetListBox" Background="Transparent" BorderThickness="0"
                                     MouseDoubleClick="PresetListBox_MouseDoubleClick"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="4,2" MinWidth="250">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                                            <Path Grid.Column="1" Data="M12,17.27L18.18,21L17.27,14.41L22,9.82L15.77,8.89L12,3L8.23,8.89L2,9.82L6.73,14.41L5.82,21L12,17.27Z"
                                                  Fill="Gold" Stretch="Uniform" Width="12" Height="12" Margin="5,0,0,0" VerticalAlignment="Center"
                                                  Visibility="{Binding IsFavorite, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                                <Button Content="名前変更" Margin="0,0,5,0" Click="RenamePresetButton_Click" IsEnabled="{Binding ElementName=PresetListBox, Path=SelectedItem, Converter={StaticResource NotNullConverter}}"/>
                                <Button Content="削除" Click="DeletePresetButton_Click" IsEnabled="{Binding ElementName=PresetListBox, Path=SelectedItem, Converter={StaticResource NotNullConverter}}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Popup>
        </Grid>

        <Grid Name="EditorGrid" Height="{Binding EditorHeight, Source={x:Static local:EqualizerSettings.Default}}" Margin="0,0,5,0">
            <Border BorderBrush="#555" BorderThickness="1" Background="#282828">
                <Canvas Name="MainCanvas" ClipToBounds="True" Background="Transparent"
                        ContextMenuOpening="MainCanvas_ContextMenuOpening"
                        ToolTip="右クリックでポイントの追加/削除ができます。"/>
            </Border>
            <Thumb Name="ResizeThumb" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Width="15" Height="15" Cursor="SizeNWSE"
                   DragStarted="ResizeThumb_DragStarted" DragDelta="ResizeThumb_DragDelta" DragCompleted="ResizeThumb_DragCompleted"
                   ToolTip="エディタの高さをドラッグして変更します。">
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#666" CornerRadius="2">
                            <Path Data="M 8,10 L 10,8 M 5,10 L 10,5 M 2,10 L 10,2" Stroke="White" StrokeThickness="1"/>
                        </Border>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
        </Grid>
        <StackPanel Margin="0,5,0,0">
            <TextBlock Text="時間軸" ToolTip="キーフレームアニメーションの現在時間を示します。"/>
            <Slider Name="TimeSlider" Minimum="0" Maximum="1" Value="0" TickFrequency="0.1"
                    ValueChanged="TimeSlider_ValueChanged"
                    ToolTip="キーフレームアニメーションの現在時間を変更します。"/>
        </StackPanel>

        <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
            <c:PropertiesEditor Target="{Binding ElementName=this, Path=SelectedBand}"
                                BeginEdit="Band_BeginEdit" EndEdit="Band_EndEdit"/>
        </ScrollViewer>
    </StackPanel>
</UserControl>